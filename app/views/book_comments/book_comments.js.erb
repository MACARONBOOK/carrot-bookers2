<!--id="comments_areaにをrenderで指定した部分テンプレートに更新-->
$("#comments_area").html("<%= j(render 'comments/book_comments', book: @book, book_comment: @book_comment) %>");
<!--フォームを送信したあとにフォームの値を空にします-->
$("textarea").val('');
<!-- フラッシュメッセージも非同期化 -->
$('#flash-message').html("<%= j(render 'layouts/flash') %>");